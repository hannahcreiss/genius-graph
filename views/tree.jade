html
  head
    title=title
    link(href='../../libs/vis/vis.css', rel='stylesheet', type='text/css')
    link(href='//netdna.bootstrapcdn.com/bootstrap/3.1.1/css/bootstrap.min.css', rel='stylesheet')
    script(type='text/javascript', src='../../libs/vis/vis.js')
     style(type='text/css').
      body {
        background-color:
      }
      #mynetwork {
      width: 600px;
      height: 400px;
      border: 1px solid lightgray;
      }
  body      
    div.container
      div.jumbotron
        h2 My Genius Graph
        form.form-horizontal(method='post', role='form')
         div.form-group
          label.col-sm-4 Artists
          div.col-sm-8
            input.form-control(
              placeholder='My faves',
              required=true,
              name='artistString',
              type='text',
              value=artistString)
         div.col-sm-offset-4.col-sm-8
            button.login.btn.btn-primary(type='submit') Submit      

    #mynetwork
    script(type='text/javascript').
      if (!{set} == true)
        var nodes = !{JSON.stringify(nodes)};
        var edges = !{JSON.stringify(edges)};
        nodes = new vis.DataSet(nodes);
        edges = new vis.DataSet(edges);
        // create a network
        var container = document.getElementById('mynetwork');
        // provide the data in the vis format
        var data = {
        nodes: nodes,
        edges: edges
        };
        var options = {
          nodes:{
            shape: "circle"

          }
        };
        // initialize your network!
        var network = new vis.Network(container, data, options);
      
      
    
